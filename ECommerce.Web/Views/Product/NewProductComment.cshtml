@model ECommerce.Web.DTOs.SaveProductCommentDto

@{
    ViewData["Title"] = "NewProductComment";
}

<h1>NewProductComment</h1>

<h4>SaveProductCommentDto</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="NewProductComment" asp-route-productId="@Model.ProductId">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="CommentText" class="control-label"></label>
                <input asp-for="CommentText" class="form-control" />
                <span asp-validation-for="CommentText" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
